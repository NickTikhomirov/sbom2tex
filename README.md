
# sbom2tex: CycloneDX SBoM to LaTeX (to PDF)

## Фичи

Главная фича: **трансляция CycloneDX SBoM в LaTeX (и, как следствие, в PDF);**
+ Поддержка ФСТЭКоSBoM-ов (по ныне публичной Методике ВУ и НДВ);
+ Полное представление о DependencyTrack-проекте за счёт слияния SBoM-ов `inventory`, `vdr` и исходного SBoM (обогащённого ФСТЭКосвойствами);
+ Настройка титульника через флаги

### Остальные фичи

+ Возможность разбить отчёт на несколько частей;
+ Оптимизация размера отчёта через сокрытие несущественных компонентов;
+ Возможность отбросить компоненты с типами `file`, `cryptographic-asset`, `operating-system`, `container` -- флаги `--drop-buzz` и `--drop-obom`;
+ Гибкое управление директивными зависимостями в отчёте:
  + Изменение глубины понятия директивности (вместо L1 можно поставить любую глубину);
  + Добавление рёбер в обход компонентов с типом `operating-system` -- для расширения списка директивных зависимостей списком пакетов ОС;
+ Упрощение ревью результатов композиционного анализа:
  + Корреляция ПА с незакрытыми CVE -- см. флаги `-S` (`--review-attack-surface`) и `-SS`;
  + Цветовая подсветка незаполненных важных мест -- см. флаг `--shame`;

## Рекомендации по использованию

### И как это использовать??

Генератор принимает SBoM-файлы. Рекомендуется брать два файла -- `inventory` и `vdr` из Dependency Track.
На SBoM-файлы накладываются следующие ограничения:
+ Генератор не распознает проблемы с коллизиями `bom-ref`;
+ Генератор берёт только компоненты из поля `.components` SBoM-а -- если у вас SBoM закодирован списками смежности в ООП-стиле (используется поле `.components` в теле других компонентов), то оно будет проигноировано;

Выходом генератора является файл (или файлы) в формате XeLaTeX, который(ые) далее можно скомпилровать в PDF.
Файлы независимы с точки зрения XeLaTeX и могут быть скомпилированы в любом порядке друг относительно друга.


#### Пошаговый пример для Depenency Track

1. Берём наш крутой обогащённый SBoM-файл с `GOST:attack_surface`, `GOST:security_function` и пр. -- назовём этот файл `A.json`
2. Загружаем `A.json` в Dependency Track, находим CVE, размечаем, оставляя комментарии (В ПРАВИЛЬНОМ ПОЛЕ!!)
3. Выгружаем файлы - - назовём их `inventory.json` и `vdr.json`
4. Вызываем генератор отчётов командой `python3 ./main.py -i vdr.json -i inventory.json -i A.json -o ./report/ --name "Проект A"`
5. Собираем выгруженный отчёт с помощью XeLaTeX.

### Что нужно для работы?

Для самого генератора не нужно ничего (кроме Python 3), но для LaTeX нужны кое-какие пакеты, перечисленные далее.

### Настройка титульного листа

Базовая настройка титульного листа реализуется через флаги. Например:
```shell
python.exe .\main.py -i ..\vdr.json -o ../test --name="Отчёт о результатах композиционного анализа проекта ПАК <<Медуза-Литерал>> (исполнение Капит
ошка-4 с аппаратным ускорителем)" -t d -t "Часть 2" -t "Лаборатория N 15408" -t "Эксперт Иванов И.И." -t "amogus"
```

Доступны флаги:
+ ``--name`` -- имя документа
+ ``-t`` -- добавление подзаголовка, для этого флага арезервированы значения:
  + ``-t d`` -- текущая дата генерации
  + ``-t t`` -- timestamp из первого переданного SBoM
  + ``-t s`` -- небольшая полосочка справа
  + ``-t r`` -- для одной строки (следующего флага `-t`) ставится выравнивание по правому краю
  + ``-t N`` -- для одной строки (следующего флага `-t`) ставится выравнивание по правому краю


### Критерии интересности компонентов

Генератор отбрасывает большую часть компонентов, поскольку нет смысла печатать 900 страниц с кучей библиотек за десятое колено транзитивности.
Остаются только компоненты, у которых есть хотя бы одно из свойств:
+ Имеется поле `GOST:provided_by` (этот параметр можно отключить через флаг `--provided-by-is-not-interesting`)
+ Поле `GOST:security_function` имеет любое значение кроме `no`
+ Поле `GOST:attack_surface` имеет любое значение кроме `no`
+ Имеется хотя бы одна CVE
+ Тип компонента отличается от стандартного набора `application`, `library` или `framework`
+ У компонента отсутствует правильный сурс (должно быть хотя бы одно из полей -- `GOST:provided_by` или `externalReferences` со значением `vcs` или `source-distribution`)

Также запланирован механизм принудительной простановки интересности компонентов, но реализации пока нет.

## Использованные библиотеки для LaTeX

В качестве фреймворка компиляции используется XeLaTeX. Не рекомендуется заменять его на аналоги, потому что все они чуть-чуть различаются, и полученный результат может отличаться от авторской задумки.
Для успешной сборки также нужны пакеты:
+ `longtable` -- пакет для работы с многостраничными таблицами;
+ `xcolor` -- пакет для работы с цветом;
+ `hyperref` -- пакет для гиперссылок (в документе нет гиперссылок кроме `\url`);
+ `xurl` -- пакет для продвинутой команды `\url`, которая позволяет переносить ссылки в любом месте;
+ `fontspec` -- пакет для настройки размера и типа шрифта;
+ `indentfirst` -- пакет для красной строки на первом абзаце в каждой главе (LaTeX по умолчанию добавляет красную строку всем абзацам кроме первого);
+ `multirow` и `multicol` -- пакеты для объединения ячеек в таблице (на данный момент multirow в проекте не используется);
+ `seqsplit` -- пакет для переноса текста "где угодно";
+ `geometry` -- пакет для настройки размера бумаги, полей и всего такого
+ `tcolorbox` -- для оформления заголовка на титульнике
+ `tikz` -- для рамки на титульнике (и да, я в курсе, что тащить tikz ради всего одной рамки на титульнике -- это одно из самых страшных преступлений в LaTeX-комьюнити)
+ ... а также соответствующие транзитивные зависимости.
